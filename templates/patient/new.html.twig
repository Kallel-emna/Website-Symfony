{% extends 'base(back).html.twig' %}

{% block title %}New Patient{% endblock %}

{% block body %}
    <h1 align = "center">Create new Patient</h1>

    {{ include('patient/_form.html.twig') }}

   <button> <a href="{{ path('app_patient_index') }}">back to list</a></button>
{% endblock %}
