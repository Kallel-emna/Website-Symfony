{% extends 'base(back).html.twig' %}

{% block title %}Edit ReservationChambre{% endblock %}

{% block body %}
    <h1>Modifier ReservationChambre</h1>

    {{ include('reservation/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_reservation_chambre_index') }}">Retour à la liste</a>

    {{ include('reservation/_delete_form.html.twig') }}
{% endblock %}
